/**
 * o segundo ! retorna o valor real que o valor tem,
 * sendo assim a maneira mais rápida de verificar se 
 * um valor é verdadeiro ou falso no JavaScript
 */

let a = "Tem algum valor";
let b;
if(!!a) {
    console.log("A variável a tem valor");
}
if(!!b) {
    console.log("A variável a tem valor");
}